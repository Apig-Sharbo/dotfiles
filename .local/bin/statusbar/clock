#!/bin/bash

## Run this with sh (dash for example) for fastest performance
# date '+%b %d (%a) %I:%M%p'

# printf 'bash version: %s\n' "${BASH_VERSION}"

## This is way faster with bash(>=4.2) since printf is bash builtin
printf '%(%b %d (%a) %I:%M%p)T' -1
